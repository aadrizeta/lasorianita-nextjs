services:
  app:
    image: ghcr.io/aadrizeta/lasorianita-nextjs:latest
    container_name: lasorianita
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - proxy

networks:
  proxy:
    external: true
    name: matching-matcha-web-nextjs_default
